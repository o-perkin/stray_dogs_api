$('#favorite_link').text("<%= favorite_text_for_show %>");
$("#favorite-<%= params[:dog]%>").text("<%= current_user.favorites.find_by_dog_id(params[:dog]) ? 'Remove from favorites' : 'Add to favorites' %>");

 